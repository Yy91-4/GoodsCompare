# GoodsCompare 开发记录 — 2026-06-05

## 回顾

上次（6月3日）完成项目脚手架的搭建，包括：
- Flask 服务端、数据库模型、三平台爬虫适配器
- 前端 PWA（HTML/CSS/JS + Service Worker）
- 单元测试骨架

## 本次进展

### 1. 推送至 GitHub
- 生成 SSH 密钥 (`ed25519`)，添加到 GitHub 账号
- `git push -u origin master` 推送到 `git@github.com:Yy91-4/GoodsCompare.git`
- 提交了 `conversation-log.md`（头脑风暴记录）

### 2. 测试修复（21 个失败 → 全部通过）
- **根因 A**: SQLite `:memory:` 模式下每次 `get_db()` 创建独立连接，`init_db()` 关闭后表丢失
  - 修复：测试改用临时文件数据库 `test_temp.db`，测试结束后清理
  - 同时在 `models.py` 中为 `:memory:` 模式使用 `shared` 缓存 URI
- **根因 B**: 淘宝搜索 URL 测试断言原始中文出现在 URL 中，但查询词已被 URL 编码
  - 修复：改为断言编码后的 `%E5%8F%AF%E4%B9%90`
- 添加 `.gitignore`（`__pycache__/`, `*.pyc`, `*.db`, `.pytest_cache/`）

### 3. UI 优化
- **页面切换动画**: `fadeSlideIn` 淡入上滑，卡片带延迟交错出现
- **搜索历史标签**: 首页显示最近 6 条搜索，点击可快速重搜
- **Toast 通知**: 收藏/录入/删除等操作有顶部弹出提示
- **最低价标记**: 搜索结果中自动标记最低价卡片，右上角红色"最低"角标
- **卡片状态区分**: 验证码(黄)、无结果(灰)、错误(红) 三种不同视觉
- **手动录入优化**: 补录价格时可同时输入商品名
- 按钮过渡效果、响应式微调、收藏按钮加星标

### 4. 其他
- Python 环境位于 Trae CN 内：`C:\Users\xyy\AppData\Roaming\Trae CN\ModularData\ai-agent\vm\tools\python\python.exe`（Python 3.10.11）
- 测试结果：38 passed / 0 failed

## 当前状态

- ✅ 代码已推送 GitHub（11 个提交）
- ✅ 全部测试通过
- ✅ Flask 服务可正常运行在 http://127.0.0.1:5000
- ⏳ 待做：真实抓取逻辑完善、手机端 Termux 部署测试
